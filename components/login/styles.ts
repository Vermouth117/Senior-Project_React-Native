import { Dimensions, StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
  },
  input: {
    justifyContent: "center",
    padding: 5,
    borderRadius: 5,
    borderColor: "black",
    borderWidth: 2,
  },
  header: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
  },
  authContainer: { flex: 1, justifyContent: "center", padding: 40 },
});
